# Communication

## Issue
- 环境：Windows，具体版本待确认。
- 复现命令：`sdt-greet --name " "`
- 期望结果：空姓名不应输出问候语，并应返回非 0 退出码。
- 实际结果：仍输出 `Hello,  !`，并以 0 退出。
- 影响：空姓名输入未被正确校验。

## Commit Message
标题：Reject empty names

正文：
空姓名输入当前仍会输出问候语并以 0 退出，导致无效输入被当作正常请求处理。增加空姓名校验，发现无效输入时不输出问候语并返回非 0 退出码。

## Review
[Blocking] 当前空姓名仍被视为有效输入并返回 0，可能导致调用方误判执行成功。建议增加空姓名校验，并补充对应测试用例。
